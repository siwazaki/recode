<?php

class DiffsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('diffs')->truncate();

		$diffs = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('diffs')->insert($diffs);
	}

}
